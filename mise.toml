[tools]
node = "22"

[tasks.lint]
description = "Brand copy lint: em/en-dashes, US spelling (ported from pio-web ENG-377)"
run = "node bin/brand-lint"

[tasks.dev]
description = "Local docs preview"
run = "mint dev"
