---
title: "Unity SDK reference"
description: "The complete public API of the PIO Unity SDK."
---

<Note>Under construction. The reference is generated from the SDK source per release; it is never hand-edited.</Note>

Will cover the full public surface: `PIO`, `PioConfig`, `PioOptions`, `PioSettings`, `DecisionResult`, `PIO.Events`, and `PioError`.
