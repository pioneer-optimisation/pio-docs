---
title: "Configuration"
description: "PioConfig, PioOptions, and the PioSettings asset."
---

<Note>Under construction.</Note>

Will cover:

- The `PioSettings` asset and the Setup Wizard
- `PioConfig`: API key, account, project, action definitions
- `PioOptions`: queueing, flush interval, decision timeout, heartbeat
